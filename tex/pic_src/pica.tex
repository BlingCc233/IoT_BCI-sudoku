\documentclass[tikz,border=6pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\begin{document}
\begin{tikzpicture}[
  box/.style={draw,rounded corners=2pt,align=center,minimum width=7.2cm,minimum height=0.95cm},
  arrow/.style={-Latex,thick},
  font=\small,
]
  \node[box] (app) {Application\\\footnotesize stream (framing) / mux / UoT (datagram)};
  \node[box,below=6pt of app] (record) {Session Record (optional)\\\footnotesize AEAD record over stream};
  \node[box,below=6pt of record] (obfs) {Sudoku Obfuscation\\\footnotesize mapping + padding + layout rotation (+ packed downlink)};
  \node[box,below=6pt of obfs] (tcp) {TCP};

  \draw[arrow] (app) -- (record);
  \draw[arrow] (record) -- (obfs);
  \draw[arrow] (obfs) -- (tcp);
\end{tikzpicture}
\end{document}

