# Auto-generated LaTeX snippets

此目录由工具 `cmd/iotbci-texgen` 写入（表格 + pgfplots 图表），生成文件可直接在论文中使用：

```bash
go run ./cmd/iotbci-texgen -bench bench.json -out_dir tex/generated
```

示例论文入口：`tex/main.tex`
